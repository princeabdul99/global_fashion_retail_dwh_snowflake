
select current_account(),current_user(), current_role();
desc user gfr_pm;
show grants to user gfr_pm;
show grants to role GFR_PM_ROLE;

use role gfr_pm_role;

show warehouses;
show databases;
show schemas;